<!DOCTYPE html>
<html lang="en-us">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}willem-jan.net shop{% endblock %}</title>
    {% stylesheets filter='cssrewrite,?yui_css' output='css/all.css'
        'bootstrap/css/bootstrap-cerulean.css'
        '@VespolinaStoreBundle/Resources/public/css/store.css'
    %}
        <link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
    {% endstylesheets %}
    <link rel="stylesheet" href="{{ asset('bootstrap/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('bootstrap/css/bootstrap-responsive.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    {% javascripts output='js/main.js' filter='?yui_js' debug=true
        'http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js'
        'http://twitter.github.com/bootstrap/assets/js/bootstrap-dropdown.js'
    %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
    {% endjavascripts %}
</head>
<body>
    <div class="container">
        <div class="row-fluid">
            <header>
                {% if is_front|default(false) %}
                    <h1 class="title"><a href="/dev">shop.willem-jan.net</a></h1>
                {% else  %}
                    <a href="/dev"><span class="title">shop.willem-jan.net</span></a>
                {% endif %}
            </header>
        </div>
        <div class="row-fluid">
            <div class="span4">
                <nav>
                    {% block side_menu %}{% endblock %}
                </nav>
            </div>
            <div class="">
                {% block content %}Default{% endblock %}
            </div>
        </div>
    </div>
</body>
</html>